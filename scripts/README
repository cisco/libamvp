This directory contains scripts that configure amvp_app to work
with well-known AMVP servers.  The amvp_app program reads configuration
from environment variables.  These scripts set the appropriate 
environment variables that amvp_app requires.
